// log:set DEBUG org.openhab.binding.network
{% for username in userdata %}{% if userdata[username].phone_ip is defined %}network:pingdevice:phone{{loop.index}} "Telefon {{userdata[username].name}}" @ "Geräte" [ hostname="{{userdata[username].phone_ip}}", retry=12, timeout=10000, refreshInterval=5000 ] // max 3.0 min
{% endif %}{% endfor %}

astro:moon:local [ geolocation="{{location}}", interval=300 ]
astro:sun:local [ geolocation="{{location}}", interval=300 ]
ntp:ntp:local [ hostname="de.pool.ntp.org", refreshInterval=60, refreshNtp=30 ]
